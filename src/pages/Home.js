import React from 'react';
import Homebaner from '../components/baner/Homebaner';

const Home = () => {
    return (
        <div>
            <Homebaner title={{Name:'Rifat', Age:22, Gender:'Male'}}/>
        </div>
    );
};

export default Home;